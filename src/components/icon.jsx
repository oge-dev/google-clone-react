import React from 'react';

// Icon Component definition
const Icon = ({ src, alt }) => {
  return (
    // Image tag with src and alt attributes
    <img src={src} alt={alt} />
  );
};

export default Icon;
